### 3. Save and Add `README.md` to Git

1. Save `README.md`.
2. Open the terminal, navigate to your project folder, and add the new file:
   ```bash
   git add README.md
   git commit -m "Add README file"
   git push origin main
